import 'package:flutter/material.dart';

class SignupPics extends StatelessWidget {
  const SignupPics({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
